**Отобразить открытые порты UDP**

```
sudo netstat -unlp
```
- `-u` — UDP порты
- `-n` — цифровые адреса (не имена)
- `-l` — только слушающие (listening)
- `-p` — PID/имя программы (требует `sudo`)

