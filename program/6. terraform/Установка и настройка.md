***Настройка автодополнения команд:***

```
terraform -version && terraform -install-autocomplete
```

**Отключение предупреждения**

> [!NOTE]
> Warning: Cannot connect to YC tool initialization service. Network connectivity to the service is required for provider version control.

```
export YC_DISABLE_TELEMETRY=1
```

