
```
sudo blkid /dev/sdb1 | cut -d' ' -f2 | xargs -I {} echo "#{}" | sudo tee -a /etc/fstab
```

```
sudo cat /etc/zabbix/zabbix_agent2.conf | grep -v '^[#[:space:]]' | grep -vP "^\s*$"
```

```
sudo find /etc/ -name www.conf 2> /dev/null
```

```
sudo du -sh -t 100M /* 2>/dev/null | sort -n -k 5 -r
```

```
bru --version ; java -version ; npm -v ; node --version ; curl -V | head -n 1 ; git --version ; gitlab-runner --version | head -n 1
```